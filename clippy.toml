disallowed-methods = [
  { path = "std::option::Option::unwrap_or", reason = "use explicit match instead of silent defaults" },
  { path = "std::option::Option::unwrap_or_else", reason = "use explicit match instead of silent defaults" },
  { path = "std::option::Option::unwrap_or_default", reason = "use explicit match instead of silent defaults" },
  { path = "std::option::Option::map_or", reason = "use explicit match instead of silent defaults" },
  { path = "std::option::Option::map_or_else", reason = "use explicit match instead of silent defaults" },
  { path = "std::result::Result::unwrap_or", reason = "use explicit match instead of silent defaults" },
  { path = "std::result::Result::unwrap_or_else", reason = "use explicit match instead of silent defaults" },
  { path = "std::result::Result::unwrap_or_default", reason = "use explicit match instead of silent defaults" },
  { path = "std::result::Result::map_or", reason = "use explicit match instead of silent defaults" },
  { path = "std::result::Result::map_or_else", reason = "use explicit match instead of silent defaults" },
]
